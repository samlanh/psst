<?php
	$tr = Application_Form_FrmLanguages::getCurrentlanguage();
	echo $this->headTitle($tr->translate("FEE"));
	$frm =  $this->form_search;
	
	$url_copy =$this->url(array('module'=>'accounting','controller'=>'fee','action'=>'copy'));
	$formList = new Application_Form_Frmtable();
	$urlCopy = $this->baseUrl()."/accounting/fee/copy/id/";
	$urlEdit = $this->baseUrl()."/accounting/fee/edit/id/";
?>	

<script type="text/javascript">
	dojo.require("dojo.data.ObjectStore");
	dojo.require("dojo.data.ItemFileWriteStore");  
	////dojo.require('dojox.form.BusyButton');
var aca_year= getDataStorefromJSON('id','name', <?php print_r(Zend_Json::encode($this->rows_year));?> );
	dojo.ready(function(){
	new dijit.form.FilteringSelect({
	    store: aca_year,
	    autoComplete: true,                        
	    required: false,
	    id: "year",
	    name: "year",           
	    class: 'fullside',  
	    placeHolder:"<?php echo $tr->translate("SELECT_YEAR");?>",          
	    onChange: function() {  
	    }
	}, "year");
	dijit.byId('year').attr('value','<?php echo $this->row_ace['year']?>');
});
</script>
<div class="card pb-10 pt-10 pl-10 pr-10">
	<div class="card-content collapse show">
		<div class="card-box">
			<form id="list" name="list" action="<?php echo $this->url(array('module'=>'accounting','controller'=>'fee','action'=>'index')); ?>" dojoType="dijit.form.Form" method="post">
				<div class="form-group">
					 <div class="col-md-2 col-sm-2 col-xs-12">
					 	<input dojoType="dijit.form.TextBox"   id="txtsearch" name="txtsearch" placeholder="<?php echo $tr->translate("SEARCH");?>....." value="<?php echo $this->adv_search['txtsearch']; ?>" type="text" class="fullside">
					 </div>
					  <div class="col-md-2 col-sm-2 col-xs-12">
					  	<input id="year" />
					 </div>
					 <div class="col-md-2 col-sm-2 col-xs-12">
					  	<?php echo $frm->getElement("branch_id");?>
					 </div>
					  <div class="col-md-2 col-sm-2 col-xs-12">
					  	<select name="finished_status" id="finished_status" dojoType="dijit.form.FilteringSelect" class="fullside" >
					 		<option value="" <?php if($this->adv_search['finished_status']==""){echo 'selected="selected"';}?>><?php echo $tr->translate("PROCESS_TYPE");?></option>
					 		<?php if(!empty($this->pro_type)) foreach ($this->pro_type As $rs){?>
					 		<option value="<?php echo $rs['id']?>" <?php if($this->adv_search['finished_status']==$rs['id']){echo 'selected="selected"';}?>><?php echo $rs['name']?></option>
					 	    <?php }?>
					 	</select>
					 </div>
					  <div class="col-md-2 col-sm-2 col-xs-12">
					  	<?php echo $frm->getElement("status_search");?>
					 </div>
					 <div class="col-md-2 col-sm-2 col-xs-12">
					 	<button iconclass="dijitIconSearch" dojoType="dijit.form.Button" showLabel="true" name="search"  type="submit"><?php echo $tr->translate("SEARCH");?></button>
					 </div>
				</div>
			</form>
		</div>
		<div class="card-box">
			<?php echo $this->list; ?>
		</div>
	</div>
</div>

<script type="text/javascript">
dojo.require("dijit.form.CheckBox"); 
dojo.require("dijit.form.NumberTextBox");
dojo.require("dijit.form.Textarea");
dojo.require("dijit.form.DateTextBox");

	function copyRecord(url){
		 id = dijit.byId('recordcopy').get('value');
		 if(id!=''){
			window.location.href = url+'/id/'+id;
		 }else{
			alert('Please select record you want to copy');
			return false;
		 }
	}

	function setValue(index){
		var recordcopy = $('input[name=copy]:checked').val();
		dijit.byId('recordcopy').attr('value',recordcopy);
	}
	var row=0;
	var url="";
	$(function(){
		$.contextMenu({
			selector: '.context-menu-one', 
			callback: function(key, options) {
			
				var m = "clicked: " + key;
				if(key=="edit"){
					url='<?php echo $urlEdit;?>';
				}else if(key=="copy"){
					url='<?php echo $urlCopy;?>';
				}
				
				gotoAction();
				//window.console && console.log(m) || alert(m); 
			},
			items: {
				"edit": {name: "<?php echo $tr->translate("EDIT");?>", icon: "edit", accesskey: "e"},
				"copy": {name: "<?php echo $tr->translate("COPY");?>", icon: "copy"},
			}
		});
	});
	function setrowdata(index){
		row = index;
	}

	var recordid ='';
	function gotoAction(){
		window.location.href =url+row;
		 //window.open(url+row);
	}
</script> 
 
 