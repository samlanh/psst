<?php
	$tr = Application_Form_FrmLanguages::getCurrentlanguage();
	echo $this->headTitle($tr->translate("ADD_SCORE"));
	$row_sub=$this->rows_sub;
  	$core_id= $this->row_score;
	//print_r($this->rows_scor);
	 $score = $this->rows_detail;
	//print_r($this->row_g);
?>	
<head>
  <meta charset="utf-8">
  <title>jQuery UI Accordion - Default functionality</title>
  <link rel="stylesheet" href="//code.jquery.com/ui/1.11.4/themes/smoothness/jquery-ui.css">
  <script src="//code.jquery.com/jquery-1.10.2.js"></script>
  <script src="//code.jquery.com/ui/1.11.4/jquery-ui.js"></script>
  <link rel="stylesheet" href="/resources/demos/style.css">
</head>

<?php
	$tr = Application_Form_FrmLanguages::getCurrentlanguage();
	//echo $this->headTitle($tr->translate('ADD_FEE')); 
	$payment_term = $this->payment_term;
	$rs_sub=$this->rows_sub;
	
?>	

<script>
dojo.require("dojo.NodeList-manipulate");
dojo.require("dojo.parser");
dojo.require("dijit.layout.AccordionContainer");
dojo.require("dijit.layout.ContentPane");
dojo.require("dijit.form.NumberTextBox");
dojo.require("dijit.form.DateTextBox");
dojo.require("dojo.data.ItemFileWriteStore");  
dojo.require("dijit.form.DateTextBox");
</script>
<style>
select{ width:100%;}
fieldset{  background:none;}
table tr.head-title{ background: none repeat scroll 0% 0% rgba(213, 249, 244, 1); padding:2px;}
table tr td.null-border{ background: #fff; border-left:none !important;}
table .set-style td,table .set-style th{ padding: 0px; border:1px solid #ccc;font-size: 12px;}
table .set-style tr.st1{ background: none repeat scroll 0% 0% rgba(218, 250, 255, 1);}
</style>
<style>
ul.blogscoresubject li{list-style: none;width:10%;float: left; }
ol.blogscoresubject li{list-style: none;width:23%;float: left; }
ol{margin: 10px;}
</style>
<script type="text/javascript">
dojo.require("dojo.data.ObjectStore");
dojo.require("dojo.data.ItemFileWriteStore");  
dojo.require("dojo.store.Memory");
dojo.require("dojo.data.ObjectStore");
dojo.require('dojox.form.BusyButton');
dojo.ready(function(){
	oldStudentScore();filterGrade();
	//control study year
		var years_store  = getDataStorefromJSON('id','name', <?php print_r(Zend_Json::encode($this->row_year));?> );
		new dijit.form.FilteringSelect({
		    store:years_store,
		    autoComplete: true,                        
		    required: true,
		    id: "year_study",
		    name: "year_study",           
		    class: 'fullside',  
		    required:'true',
		    placeHolder:"Selected Study Years",          
		    onChange: function() {  
			   
		    	filterStudentByCondition();
		    }
		}, "year_study");
		
		new dijit.form.FilteringSelect({
			//store: dept_store,
			autoComplete: true,                        
			id: "grade",
			name: "grade",           
			class: 'fullside', 
			placeHolder:"Selected Grade",          
			onChange: function() {
				filterStudentByCondition();
			}
		}, "grade");
		
		var group_store  = getDataStorefromJSON('id','name', <?php print_r(Zend_Json::encode($this->rows_group));?> );
		new dijit.form.FilteringSelect({
			store:group_store,
		    autoComplete: true,                        
		    required: true,
		    id: "study_group",
		    name: "study_group",           
		    class: 'fullside',  
		    required:'',
		    placeHolder:"Selected Study Group",          
		    onChange: function() {  
		    	study_group=dijit.byId('study_group').get('value');
		    	//document.getElementById("table_row").innerHTML = '';
		    	getStudentAll();
				  //  getStudent(study_group);
				  //oldStudentScore();
			    getParentName();
		    }
		}, "study_group");

		dijit.byId('year_study').attr('value','<?php echo $this->row_g['academic']?>');
		 
	});
 
var grade_store  = getDataStorefromJSON('id','name', <?php print_r(array())?> );
var url_dept = '<?php echo $this->url(array('module'=>'foundation','controller'=>'studentscore','action'=>'get-grade')); ?>';
function filterGrade(){
	degree = dijit.byId('degree').get('value');
		dojo.xhrPost({
			url:url_dept,
			content:{
				'degree':degree
				},
			handleAs:"json",
			load: function(data) {
				//alert(data);
				grade_store  = getDataStorefromJSON('id','name', data);
			    dijit.byId('grade').set('store',grade_store);  
				dijit.byId('grade').attr('value','<?php echo $this->row_g['grade']?>');
			    //if(a!=""){
			    //	dijit.byId('grade').attr('value',a);
		        //}
			},
			error: function(err) {
				//alert(err);
			}
		});
}

var url_getStudent= '<?php echo $this->url(array('module'=>'foundation','controller'=>'studentscore','action'=>'get-student')); ?>';
function filterStudent(){
	//alert(1);
	year = dijit.byId('year_study').get('value');
	grade = dijit.byId('grade').get('value');
	session = dijit.byId('session').get('value');

	if(year==''){
		return false;
	}
	if(grade==''){
		return false;
	}
	if(session==''){
		return false;
	}
	dojo.xhrPost({
	    url: url_getStudent,
	    content : { 
		    'year':year,
			'grade':grade,
			'session':session,
		},				    
	   handleAs:"json", 
	   load: function(data) {
		   tem="";
	       if(data!=""){
				num =0;
				title=0;
				temp='';
				str='';
				column = 0;
				    str+='<div id="accordion">';
				for(i=0;i<data.length;i++){ //alert(data[i].stu_id);
					inx = i+1;
					num++;
					str+='<h3>'+inx+', Student id('+data[i].stu_code+')'+'&nbsp;|&nbsp;&nbsp;Student Name('+data[i].stu_name+')&nbsp;|&nbsp;&nbsp;Sex('+gender(data[i].sex)+')&nbsp;|&nbsp;<input   type="hidden" name="stu_id_'+inx+'" id="stu_id_'+inx+'" value="'+data[i].stu_id+'"/></h3>';
					str+='<div >';
					str+='<center><ul class="blogscoresubject" style="width:100%;">';
						
					<?php   $db = new Global_Model_DbTable_DbHomeWorkScore();?>
					<?php if (!empty($this->rows_parent)) foreach ($this->rows_parent As $key =>$rs_parent){ $key=$key+1; ?>
					
                    str+='<li style="white-space: nowrap;text-align:left;padding:0px 5px; width:18%; font-size:12px;margin-top:8px;margin-left: 20px;"><?php echo $key.".".$rs_parent['sub_name']?></li>';
                    str+='<li><input   dojoType="dijit.form.validateNumberTextBox" class="fullside" name="<?php echo str_replace(' ','',$rs_parent['subject_titleen'])?>'+inx+'" id="<?php echo $rs_parent['subject_titleen']?>'+inx+'"   value="" type="text" onkeypress="return AllowOnlyNumbers(event);"></li>';
                   
					<?php foreach ($db->getSubject($rs_parent["id"]) as $rs){ ?>
                           str+='<li style="white-space: nowrap;text-align:left;padding:0px 5px; width:10%;font-size:12px;">&nbsp;&nbsp;-&nbsp;<?php echo $rs['sub_name']?></li><li><input dojoType="dijit.form.validateNumberTextBox" class="fullside" name="<?php echo str_replace(' ','',$rs['subject_titleen']) ?>'+inx+'" id="<?php echo $rs['subject_titleen']?>'+inx+'"   value="" type="text" onkeypress="return AllowOnlyNumbers(event);"></li>';
					<?php }?>
					 
                   <?php }?>

                  
                   
					str+='</ul></center>';
					str+='</div>';
				    if(inx!=1) {//identity set value 1,2,3,4,5
						var identity = $('#identity').val();
						$('#identity').val(identity+','+inx);
					} else {
						$('#identity').val(inx);
					}
				}
				str+='</div>';
				document.getElementById("liststudent").innerHTML = str;
				 $( "#accordion" ).accordion();
				 
				
	       }else{
	    	   $('#identity').val('');
	    	   document.getElementById("liststudent").innerHTML = '';
	       }
	   },		
	    error: function(err) {
		 //   alert(err);
	    }
	});
	
}

function getStudentAll(){
	study_group=dijit.byId('study_group').get('value');
	if(study_group !=''){
		//getStudentByIdGroup(study_group);
		getStudentGroup();
	} 
}
//get parent name filter by group id
function getParentName(){
	group_id=dijit.byId('study_group').get('value');
	if(group_id !=''){
		getParent(group_id);
	}
}
var parent_store  = getDataStorefromJSON('id','name', <?php print_r(array())?> );
var url_parent = '<?php echo $this->url(array('module'=>'foundation','controller'=>'studentscore','action'=>'get-parent-name')); ?>';
function getParent(group_id){
	dojo.xhrPost({
		url:url_parent,
		content:{
			'group_id':group_id,
			},
		handleAs:"json",
		load: function(data) {
			parent_stores  = getDataStorefromJSON('id','name', data);
		 //   dijit.byId('parent_name').set('store',parent_stores);
		},
		error: function(err) {
			  alert(err);
		}
	});
}

var old_academic = '<?php echo $this->row_g['academic'];?>';
var old_grade = '<?php echo $this->row_g['grade'];?>';
var old_session = '<?php echo $this->row_g['session'];?>';

function filterStudentByCondition(){
	
	var new_academic = dijit.byId('year_study').get('value');
	var new_grade = dijit.byId('grade').get('value');
	var new_session = dijit.byId('session').get('value');
	
	if(old_academic ==new_academic &&  old_grade ==new_grade &&  old_session== new_session){
		oldStudentScore();
	}else{
		//alert(old_academic + ' = '+ new_academic + ' , ' + old_grade + ' = '+ new_grade + ' , ' + old_session + ' = '+ new_session);
		filterStudent();
	}
}
function oldStudentScore(){ 
	str='';
	str+='<div id="accordion">';
		<?php 
			$old_stu = null;
			$i=0;
			foreach ($score as $key=>$rs){
				if($old_stu != $rs["student_id"]){
				$i = $i+1;
		?>
		inx = '<?php echo $i; ?>';
		str+='<h3>'+inx+', Student id(<?php echo $rs["stu_code"] ?>)'+'&nbsp;|&nbsp;&nbsp;Student Name(<?php echo $rs["student_name"] ?>)&nbsp;|&nbsp;&nbsp;Sex('+gender(<?php echo $rs["sex"] ?>)+')&nbsp;|&nbsp;<input   type="hidden" name="stu_id_'+inx+'" id="stu_id_'+inx+'" value="<?php echo $rs["student_id"] ?>"/></h3><div >';
		str+='<center><ul class="blogscoresubject" style="width:100%;">';
		<?php 
			$j =0;
			foreach ($score as $rss){
				if($rs["student_id"]==$rss["student_id"]){
		?>
		<?php if($rss["is_parent"]== 1){ $j=$j+1;?>
			index = '<?php echo $j;?>';
			str+='<li style="white-space: nowrap;text-align:left;padding:0px 5px; width:18%;font-size:12px;margin-top:8px;margin-left: 20px;"><?php echo $j.".".$rss['subject_name']?></li>';
	    	str+='<li><input   dojoType="dijit.form.validateNumberTextBox" class="fullside" name="<?php echo str_replace(' ','',$rss['subject_titleen'])?>'+inx+'" id="<?php echo $rss['subject_titleen']?>'+inx+'"   value="<?php echo $rss["score"];?>" type="text" onkeypress="return AllowOnlyNumbers(event);"></li>';
	    <?php }else{?>
	    str+='<li style="white-space: nowrap;text-align:left;padding:0px 5px; width:25%;font-size:12px;">&nbsp;&nbsp;-&nbsp;<?php echo $rss['subject_name']?></li><li><input dojoType="dijit.form.validateNumberTextBox" class="fullside" name="<?php echo str_replace(' ','',$rss['subject_titleen']) ?>'+inx+'" id="<?php echo $rss['subject_titleen']?>'+inx+'"   value="<?php echo $rss["score"] ?>" type="text" onkeypress="return AllowOnlyNumbers(event);"></li>';
		<?php }}}?>

		str+='</ul></center>';
		str+='</div>';
		if(inx!=1) {//identity set value 1,2,3,4,5
			var identity = $('#identity').val();
			$('#identity').val(identity+','+inx);
		} else {
			$('#identity').val(inx);
		}
		<?php } $old_stu=$rs["student_id"];}?>
	str+='</div>';

	document.getElementById("liststudent").innerHTML = str;
	 $( "#accordion" ).accordion();
}

function gender(sex){
	if(sex==1){
		sex='Male';
	}else sex='Female';
	return sex;
}
</script>
<form id='office_receipt' action="<?php echo $this->url(array('module'=>'foundation','controller'=>'studentscore','action'=>'edit')); ?>" dojoType="dijit.form.Form" method="post" enctype="application/x-www-form-urlencoded">
<script type="dojo/method" event="onSubmit">   
   if(this.validate()) {
    return true;
   }else {
    return false;
   }
</script>
<table cellspacing="10" style="margin: 0 auto;width:100%;" >
	<tr>
		<td>
		<fieldset>
					<legend align="center" style="font-size: 14px;"><strong><?php echo $tr->translate("ADD_NEW_STUDENT_SCORE");?></strong></legend>
					<table style="margin: 0 auto; width: 100%; background:#DFF7FD;" cellspacing="10" broder 1>
						<tr>
							<td  ><?php echo $tr->translate("STUDY_YEAR");?></td>
							<td>
								<input id="year_study" />
							</td>
							
							<td ><?php echo $tr->translate("DEGREE");?></td>
							<td>
								<select id="degree" name="degree" onchange="filterGrade();" class="fullside" dojoType="dijit.form.FilteringSelect" type="text"  >
									<option value="-1"> ------ select degree---------</option>
								<?php if(!empty($this->degree)){foreach ($this->degree as $degree){?>
									<option value="<?php echo $degree['id'];?>" <?php if($degree['id'] == $this->row_g['degree']){echo 'selected="selected"';}?>><?php echo $degree['name'];?></option>
								<?php }}?>
								</select>
							</td>
							
							<td ><?php echo $tr->translate("GRADE");?></td>
							<td>
								<input id="grade" />
							</td>
						</tr>
						<tr>
							<td ><?php echo $tr->translate("SESSION");?></td>
							<td>
								<select id="session" name="session" onchange="filterStudentByCondition();" class="fullside" dojoType="dijit.form.FilteringSelect" type="text"  >
									<option value="-1"> ------ select session---------</option>
								<?php if(!empty($this->session)){foreach ($this->session as $session){?>
									<option value="<?php echo $session['key_code'];?>" <?php if($session['key_code'] == $this->row_g['session']){echo 'selected="selected"';}?>><?php echo $session['view_name'];?></option>
								<?php }}?>
								</select>
							</td>
							
							<td><?php echo $tr->translate("Monthly Report");?></td>
							<td><input dojoType="dijit.form.DateTextBox" required="true" class="fullside" id="reportdate" name="reportdate" type="text" value="<?php echo date("Y-m-d");?>"></td>
						  
							<td style="white-space: nowrap;text-align: right;"><?php echo $tr->translate("STATUS");?></td>
							<td>
								<select dojoType="dijit.form.FilteringSelect" required="true" class="fullside" id="status" name="status" type="text">								
										<option value="1">ប្រើប្រាស់</option>
										<option value="0">មិនប្រើប្រាស់</option>
								</select>
							</td>	
						</tr>
						<tr>
							<td colspan="8">
							<labal id="liststudent">
							</labal>
							</td>
						</tr>
					</table>
					<table class="set-style" border="2px" style="border:1px solid #ccc; border-collapse: collapse;margin: 0 auto; width: 100%; background:#DFF7FD;" >
						<thead>
							<tr>
								<td colspan="8" align="left">
									<input type="hidden" name="identity" id="identity"  value="" >
								 </td>
							</tr>
							
							 
						</thead>
						<tfoot>
							<tr>
								<td colspan="8" align="center">
								 	<input iconClass="dijitIconClear" type="reset" value="ážŸáŸ†áž¢áž¶áž�" label="<?php echo $tr->translate('CLEAR');?>" dojoType="dijit.form.Button"/>
									<input type="submit" value="save_close" name="save_close" label="<?php echo $tr->translate('SAVE_CLOSE');?>" dojoType="dijit.form.Button" 
										iconClass="dijitEditorIcon dijitEditorIconSave" />
									 
								</td>
							</tr>
						</tfoot>
				 </table>
		</fieldset>		
		</td>
	</tr>
</table>	
</form>


<script> //validate controll form 
function AllowOnlyNumbers(e) {

    e = (e) ? e : window.event;
    var key = null;
    var charsKeys = [
        97, // a  Ctrl + a Select All
        65, // A Ctrl + A Select All
        99, // c Ctrl + c Copy
        67, // C Ctrl + C Copy
        118, // v Ctrl + v paste
        86, // V Ctrl + V paste
        115, // s Ctrl + s save
        83, // S Ctrl + S save
        112, // p Ctrl + p print
        80 // P Ctrl + P print
    ];

    var specialKeys = [
	    8, // backspace
	    9, // tab
	    27, // escape
	    13, // enter
	    35, // Home & shiftKey +  #
	    36, // End & shiftKey + $
	    37, // left arrow &  shiftKey + %
	    39, //right arrow & '
	    46, // delete & .
	    109,//.
	    45 //Ins &  -
    ];

    key = e.keyCode ? e.keyCode : e.which ? e.which : e.charCode;

    //console.log("e.charCode: " + e.charCode + ", " + "e.which: " + e.which + ", " + "e.keyCode: " + e.keyCode);
    //console.log(String.fromCharCode(key));

    // check if pressed key is not number 
    if (key && key < 48 || key > 57) {
        //Allow: Ctrl + char for action save, print, copy, ...etc
        if ((e.ctrlKey && charsKeys.indexOf(key) != -1) ||
            //Fix Issue: f1 : f12 Or Ctrl + f1 : f12, in Firefox browser
            (navigator.userAgent.indexOf("Firefox") != -1 && ((e.ctrlKey && e.keyCode && e.keyCode > 0 && key >= 112 && key <= 123) || (e.keyCode && e.keyCode > 0 && key && key >= 112 && key <= 123)))) {
            return true
        }
            // Allow: Special Keys
        else if (specialKeys.indexOf(key) != -1) {
            //Fix Issue: right arrow & Delete & ins in FireFox
            if ((key == 39 || key == 45 || key == 46 || key == 109)) {
                //return (navigator.userAgent.indexOf("Firefox") != -1 && e.keyCode != undefined && e.keyCode > 0);
                return true
            }
                //DisAllow : "#" & "$" & "%"
            else if (e.shiftKey && (key == 35 || key == 36 || key == 37)) {
                return false;
            }
            else {
                return true;
            }
        }
        else {
            return false;
        }
    }
    else {
        return true;
       }
    }
</script>
 